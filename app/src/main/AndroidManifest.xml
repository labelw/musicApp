<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wjy.mynewapplication">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Palyer" />
        <activity android:name=".Save" />
        <activity android:name=".Filelist" />
        <activity android:name=".SqlLite" />
        <activity
            android:name=".MusicList"
            android:theme="@style/TestName" />
        <activity
            android:name=".Index"
            android:theme="@style/TestName">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Search"
            android:theme="@style/TestName"></activity>
        <activity
            android:name=".SearchRt"
            android:theme="@style/TestName" />

    </application>

</manifest>